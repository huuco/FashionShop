<% provide :title, t("admin.list") %>
<div id="page-wrapper">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <h1 class="page-header"><%= t "admin.title" %>
          <small><%= t "admin.list" %></small>
        </h1>
      </div>
      <div class="action_btn row col-lg-12">
        <%= link_to t(".add_new"), new_admin_brand_url,
          class: "btn btn-default" %>
      </div>
      <table class="table table-striped table-bordered table-hover"
         id="dataTables-example">
        <thead>
          <tr align="center">
            <th><%= t "admin.brands.index.id" %></th>
            <th><%= t "admin.brands.index.name" %></th>
            <th><%= t "admin.brands.index.description" %></th>
            <th><%= t "admin.edit" %></th>
            <th><%= t "admin.delete" %></th>
          </tr>
        </thead>
        <tbody>
          <%= render partial: "brand", collection: @brands, as: :brand %>
        </tbody>
      </table>
      <%= paginate @brands %>
    </div>
  </div>
</div>
